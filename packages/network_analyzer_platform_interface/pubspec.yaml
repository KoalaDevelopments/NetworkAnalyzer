name: network_analyzer_platform_interface
description: >-
  A common platform interface for the network_analyzer plugin.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: '>=3.3.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  checks: ^0.3.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
