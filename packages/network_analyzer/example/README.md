# network_analyzer_example

Demonstrates how to use the `network_analyzer` plugin and hosts the
integration tests (constitution: the example app exercises every public
API feature).

```shell
flutter run                    # bootstrap probe demo
flutter test integration_test  # end-to-end round-trip on a device
```
